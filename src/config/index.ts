const config = {
  publicPaths: ['/login', '/api/login', '/api/logout'],
  uiPaths: {
    login: '/login',
    dashboard: '/dashboard',
  },
  apiPath: '/api',
  staticPaths: {
    general: '/_next',
    favicon: '/favicon.ico',
  },
}

export default config
